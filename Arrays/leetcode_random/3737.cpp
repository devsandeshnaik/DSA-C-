#include<iostream>
#include<vector>

using namespace std;

class Solution {
public:
    int countMajoritySubarrays(vector<int>& nums, int target) {
        int high, low = 0;
        int count = 0;
        int majorityEle = 0;
        while(high < nums.size()-1) {
            if
        }
    }
};

int main() {
    vector<int> vec = {1,2,2,3};
    int result = Solution().countMajoritySubarrays(vec, 2);
}